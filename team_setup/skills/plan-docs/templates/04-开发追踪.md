# 开发追踪

> 开发顺序（由 `03-索引.md` 依赖推导）+ 状态看板。状态实时更新。

## 开发顺序
> 被依赖者先做、底层先于上层。拓扑排序结果。

1. [[_B]] 模块B（无依赖，先做）
2. [[_A1a]] 子模块A1a
3. [[按钮X]]（依赖 B-流程Y）
4. ...

## 状态看板
| 顺序 | 模块/任务 | 文档 | 状态 | 备注 |
|------|----------|------|------|------|
| 1 | 模块B | [[_B]] | 待开发 | |
| 2 | 子模块A1a | [[_A1a]] | 待开发 | |
| 3 | 按钮X | [[按钮X]] | 受阻 | 等待 B-流程Y |

> 状态取值：待开发 / 进行中 / 已完成 / 受阻（受阻须注明阻塞原因）。

## 循环依赖告警
> 若存在循环依赖，列在此处并提示用户拆解。
