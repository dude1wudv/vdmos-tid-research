# 索引（单一事实来源）

> 全局导航。任何模块结构的增删改，**先改这里**，再动具体文档。
> 缩进 = 纵向父子关系；依赖总表 = 横向调用关系。

## 模块树（纵向：包含/父子）
> 缩进表示层级，可无限递归。每个节点链接到其文档。

- [[_A]] 模块A
  - [[_A1]] 子模块A1
    - [[_A1a]] 子模块A1a
    - [[按钮X]]
  - [[_A2]] 子模块A2
- [[_B]] 模块B

## 横向依赖总表（依赖/调用）
| 源 | 依赖 | 说明 |
|----|------|------|
| [[按钮X]] | [[_B-流程Y]] | 点击后触发 B 的流程 Y |

## 阅读/开发建议顺序
> 见 `04-开发追踪.md` 的开发顺序区（由本表的依赖推导）。
