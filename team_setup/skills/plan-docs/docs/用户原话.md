# 用户原话记录

> 本文档逐字记录用户的原始需求与表述，作为后续技能设计的依据。
> 不做改写、不做总结，只忠实记录。

---

## 2026-06-14

**用户：**
> 你好，我现在想要做一个技能，请你先在当前目录创建一个 docs，然后创建一个用户原话文档用来记录我们的原话

**用户：**
> 这个技能的第一个功能就是保留用户原话文档，记录用户说的话，方便反过头来梳理

**用户：**
> 这知识其中一个功能，我还需要其他的功能

**用户：**
> 第二个功能，和ai讨论需求，梳理成相关的文档，精细到每一个功能模块甚至是点这个按钮应该是什么场景，站在产品经理的角度讨论需求和产品的实用性，而不仅仅是开发任务

**用户：**
> 1.我感觉你说的挺对的，但是没有对到细节上，很多人可能会和ai沟通大方向但是细节一点都不会梳理，这样就会导致，在整个项目的开发过程中如果上下文超限ai就会出现思绪发散和偏移的情况。为了避免这种情况的发生，所以我们需要细致到每个功能模块的实现文档，哪怕是点击这个按钮应该是什么样的一个逻辑，哪怕只有几十个字也要形成单独的文档，但不仅仅是单独的文档，而是有索引的，能够前后逻辑通顺且关联

**用户：**
> c，但是一个功能模块可能会有很多子模块，子模块下可能还会有很多模块，这就是功能模块中开发的关联关系

**用户：**
> 可以，但是对于完整的项目是不是应该有项目说明书、架构、需求文档等等？

**用户：**
> 可以，然后可以询问用户是否需要根据微观实现设计原型图

**用户（回答 AI 提问·原型形态）：**
> HTML/CSS 可交互页面（高保真原型，可在浏览器打开并点击交互）

**用户：**
> 第四个功能就是，开发追踪文档、功能开发的先后顺序的梳理

**用户：**
> 1.第五个功能，首先是因为跟ai的沟通是有代沟的，用很原始的人类的话跟他讲，理解能力和人类肯定是有差别的，所以，这个功能需要用户完全确认了微观文档和开发追踪文档后，由ai拆分细致任务，哪怕精确到一个debug语句，也要保证开发任务的细致，所以这个功能需要生成针对/goal，或者手动给ai扔提示词去完成某个小任务，或者通过goal自动逐个去完成，提示词不用特别复杂，而是能够贴合ai的理解避免出现思绪发散等问题，2.新项目引导式吧，老项目可以完善这些内容，方便用户更了解项目然后再去完善项目，老项目可以类似于一个项目初始化的功能由ai去了解并构建我们需要的文档树？

**用户：**
> codex和claudecode 都有这个功能吧？你去查一下

**AI 查证结论（功能五设计依据，非用户原话）：**
> 确认 Claude Code 与 OpenAI Codex CLI 均内置 `/goal` 命令。`/goal` = 目标驱动的自主执行模式：用户给出一个「可验证的完成条件」，AI 自主多轮执行，由独立检查模型逐轮校验，达成即停。Claude Code 于 v2.1.139（2026-05-11）上线，默认用 Haiku 做检查模型；Codex CLI 更早（v0.128.0）。
> → 故功能五的产出应为「带明确可验证完成条件的 `/goal` 提示词」，天然契合该机制并防漂移。

**用户（修正核心问题定位）：**
> 核心问题并不是防止上下文超限导致ai发散、偏移，而是约束ai和细节话功能开发

**用户（修正功能五）：**
> 并不一定必须要用goal，而是可以goal 也可以支持用户直接复制发送给ai的提示词，而且内容是ai更易理解的源数据，主要是微观化任务，细节拆分然后实现

**用户（决定下一步）：**
> 继续吧 / 2（选择：直接进入实现，按 SKILL.md + 多子文档形态搭建，细节边做边定）

---
