# 需求文档：TCAD 参考流程学习与新芯片仿真准备

## 目标

把现有 `Trench VDMOS.gzp` 参考工程整理成后续新芯片仿真的工作依据，使 AI 后续能按固定流程协助：

1. 根据芯片资料建立器件物理模型。
2. 先完成辐照前仿真，得到可对齐的初始曲线。
3. 接收辐照后的实测曲线。
4. 在 Sentaurus 中调参，拟合辐照后曲线效果。
5. 输出可复核的 deck、曲线、参数表、日志和结论。

## 当前已知事实

- 参考工程：`Trench VDMOS.gzp`。
- 工程类型：Sentaurus Workbench 工程包。
- 已有调好案例：沟槽 VDMOS / TID / Id-Vg 阈值匹配。
- 已有本地资料包：`local_runtime/vdmos_tid_match_20260709/`。
- 已有扫描记录：`docs/changes/2026-07-08-vdmos-idvg-not-nit-scan/`。

## 本次文档边界

本次只固化“参考工程学习结果 + 下一颗芯片接续流程”。不重新跑仿真，不修改 `.gzp`，不声称已经完成下一颗芯片建模。

## 后续输入

用户后续会提供新芯片文档。收到后，应先抽取：结构尺寸、材料层、掺杂/工艺信息、电极定义、测试条件、辐照条件、辐照前后实测曲线与目标指标。