上级：[[_参考工程学习]]
下级：无
依赖：无
---

# 工程包识别

`Trench VDMOS.gzp` 是 Sentaurus Workbench 工程包，内部项目名为 `CTD03N004`。

关键入口：

- `gtree.dat`：参数树，含 `pb_doping`、`Not`、`Nit`。
- `sde_dvs.cmd`：结构建模模板。
- `sdevice_des.cmd`：辐照前/基线仿真模板。
- `TID_des.cmd`：辐照后等效损伤模板。