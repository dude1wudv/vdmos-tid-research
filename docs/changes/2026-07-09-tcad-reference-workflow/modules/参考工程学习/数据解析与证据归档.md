上级：[[_参考工程学习]]
下级：无
依赖：[[辐照前仿真]]、[[TID辐照后等效模型]]
---

# 数据解析与证据归档

定量结论以 `.plt` 解析结果为准，GUI 截图只作为旁证。

最小归档清单：

- deck：实际运行的 `.cmd`。
- 数据：`IdVg_*.plt`、汇总 CSV。
- 日志：`.log` / stdout，确认 `Good Bye` 或正常完成。
- 图：Id-Vg 对比、Vth/目标窗口对比。
- 结构结果：必要的 `.tdr`。
- 报告：说明目标、设置、参数、结果、失败/边界。