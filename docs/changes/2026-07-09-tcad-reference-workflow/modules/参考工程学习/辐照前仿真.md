上级：[[_参考工程学习]]
下级：无
依赖：[[SDE物理模型]]
---

# 辐照前仿真

参考工程使用低漏压转移特性作为最小校准入口：

- `Vd=0.1 V`
- `Vg` 从负压扫到 `5 V`
- 提取 `Id-Vg` 曲线
- 用恒流法提取 `Vth`

已归档的匹配点：`pre_fit` 使用 `Not=3e10`、`Nit=1.4e11`，得到 `Vth≈2.003 V`。

后续新芯片必须先让辐照前曲线合理，否则辐照后拟合没有意义。