上级：[[_新芯片仿真流程]]
下级：无
依赖：[[新器件建模]]
---

# 辐照前曲线校准

先跑辐照前 Id-Vg，并对齐实测初始曲线或数据手册窗口。

优先调参顺序：

1. 测试条件和面积缩放：确认 Vd、Vg、温度、AreaFactor。
2. 几何和氧化层厚度：影响栅控和阈值。
3. P-body / 外延掺杂：影响阈值、导通和耗尽。
4. 基线固定电荷/界面态：只作小范围背景修正。

不要直接用大 `Not/Nit` 去掩盖初始模型错误。