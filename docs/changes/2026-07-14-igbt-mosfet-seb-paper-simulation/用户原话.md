# 用户原话

> 在 E:\VDMOS_TID_Research 中端到端执行用户已确认的“正式论文 IGBT/MOSFET SEB 仿真工程”计划。你是唯一实现 worker，允许编辑仓库与 local_runtime、连接 tcad@192.168.137.131 并运行 Sentaurus；不要提交或推送 Git。
>
> 核心约束：以 451.15 µm IGBT 结构为冻结来源，以 attempt04 的 HeavyIon 语义为来源；建立 7 个唯一 IGBT 主案与 1 个结构匹配 MOSFET 派生对照案；先网格、再目标偏压热稳态 restart、再共享 IGBT 参考瞬态；参考门失败即 fail closed。所有 SDevice 必须经 `scripts/run_igbt_seb_case.ps1`、单线程、自动 CPU 租约。主案固定 LET=15 MeV·cm²/mg、LET_f=0.1555 pC/µm、Y=3.5 µm、Length=50 µm、Wt_hi=0.1 µm、Vg=Ve=0、HeavyIon Time=0.1 ns，并保存精确总时刻 2.1 ns（注入后 2.000 ns）。
>
> 最终需要计划文档网络、唯一 `case_matrix.csv`、脚本最小扩展、原始运行证据、统一提取与渲染、小型 CSV/图/案例验收表/中文报告；严格区分验证案、8 个论文主案、通过/失败/未运行，不把 BV、2500 V A01 或普通 Plot TDR当正式结果，不提交 Git。

## 原话保真说明

本页保留本次已确认任务的核心原话和不可变约束。完整逐字请求保存在本次父会话；本页未加入新的物理假设。实现中的查证、推断和运行结果分别进入需求、架构、追踪和最终报告。