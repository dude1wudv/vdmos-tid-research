# IGBT HeavyIon/SEB 逐次运行记录

> 状态：执行中。本文件记录可公开、可审计的操作事实；原始命令、完整日志和大型场文件保存在忽略的私有证据目录。

## 1. 运行边界

- 远端隔离根：`/home/tcad/codex_runs/igbt_seb_<timestamp>/`（精确路径仅保留在私有账本）。
- 本地私有证据根：`local_runtime/igbt_seb_full_<timestamp>/`。
- 正式网格：31,769 points、62,720 elements。
- 正式网格 SHA-256：`5c7c098706bcf07dc2656a1bfd547aea3415b02167d6afb0439c0a28f35f5265`。
- 首次预注册主链在 A01 完成有界诊断后关闭；A02–A04 未启动。历史协议 PA01 的隔离并行提案未进入执行，当前由严格串行决策覆盖。

## 2. HeavyIon smoke

### S00 attempt01

- 条件：100 V、LET 10、Y=3.5 µm、2 ns。
- 结果：语法、实际偏压、输出字段和 2 ns 求解通过。
- 缺口：保存时刻位于脉冲后，不能以该 TDR 验收瞬时 `HeavyIonGeneration`。
- 处置：仅增加脉冲附近输出时刻，不改变任何物理参数。

### S00 attempt02

- 实际 VCE：100.0 V。
- 100–102 ps 的 `HeavyIonGeneration` 峰值：约 `4.84e29–6.83e29 cm^-3 s^-1`。
- 轨迹：沿 Y=3.5 µm，由表面延伸至约 49.08 µm 深度。
- 表面净掺杂：约 `6.80e19 cm^-3`，与 n+ emitter 有源区一致。
- 结论：`SMOKE_VALID`；按预注册规则不作 SEB 分类。

## 3. 正式网格

局部加密仅服务离子轨迹核心和机理观察区域，保留冻结基线已有的栅氧界面、结区、buffer/collector 加密。原栅氧界面最小边长约 0.000758 µm，未被新规则覆盖。

## 4. A01：2500 V + LET 15 + Y=3.5 µm

### attempt01：首次冻结策略观察值

- 实际 VCE：2500.0 V，偏差 0 V。
- 到达时间：33.737 ns。
- Tmax：325.571 K。
- 峰值 Collector 电流：`1.57865e-4 A/µm`。
- 峰值功率：`0.394663 W/µm`。
- 分类：`INDETERMINATE(time_window_short)`。
- 说明：未达到 1 µs，不能判为 `NO_SEB`。

### attempt02：无效 Plot TDR restart

- 调整：尝试普通 Plot TDR restart、8 线程和分段脉冲时间窗。
- 结果：启动求解失败。普通 Plot TDR 缺失一致 restart 所需状态，不能替代 `Save` 文件。
- 分类：`INDETERMINATE(numerical_failure)`。
- 决策：拒绝该 restart 路径，回到原始正式网格正常偏压爬升。

### attempt03：创建真实 restart，发现并行负加速

- 调整：原始网格正常 ramp；DC `MaxStep` 使用冻结基线已经验证过的 0.05；使用 `Save(FilePrefix="bias2500")`；8 线程。
- 实际 VCE：2500.0 V。
- 到达时间：59.583 ns。
- Tmax：338.927 K。
- 峰值 Collector 电流：`6.72640e-4 A/µm`。
- 峰值功率：`1.68160 W/µm`。
- 输出完整性：4 个保留快照均含要求字段。
- 分类：`INDETERMINATE(time_window_short)`。
- 性能观察：8 线程单步 wall time 接近 38 s，明显慢于单线程；该调整不接受为后续执行配置。

### attempt04：真实 restart + 单线程

- 唯一执行层变化：从 attempt03 的 `bias2500_des.sav` 与配套 circuit save 加载，线程数从 8 恢复为 1。
- restart SHA-256：主状态 `de91e24871e53b03c05aecb2799401a8939d270970326e1b8709c083dfb76032`；电路状态 `3f35a68b78eaacca5c4cc995ab0be8ddb49fbac9ded5e499a65e9851781fcba5`。
- 物理、网格、HeavyIon、时间步分段、终止阈值均未改变。
- 启动验收：`Load` 成功，直接以 2500 V 状态进入瞬态；早期单步约 2.7–2.8 s。
- 最终冻结：运行 14,861 s 后在 81.933 ns 正常中断；Tmax 395.229 K；峰值 Collector 电流 `3.172e-3 A/µm`；峰值功率 `7.931 W/µm`；Newton 缩步重试 876 次。
- 分类：`INDETERMINATE(time_window_short_and_stiff)`；未达到 1680 K/2500 K，不能判 SEB 或 NO_SEB。
- 电流持续增长，与论文 A01 约 10 ns 达峰后衰减的趋势不匹配。详见 `06-中期检查报告.md`。

## 5. 当前调优结论

1. 增加 strike-time snapshot 有效关闭 smoke 证据缺口。
2. 普通 Plot TDR restart 无效，已保留为负结果。
3. 求解器原生 `Save/Load` 是当前已验证的 restart 机制。
4. 该网格和该求解阶段下 8 线程产生显著负加速，单线程更合适。
5. 所有未覆盖 1 µs 且未发生已确认物理 break 的 A01 结果均维持 `INDETERMINATE`。

## 6. 输入审计与分段 restart

- attempt05 在 1800 s 预算内推进至 39.645 ns，生成 0.4/10/20 ns 真 Save。
- attempt06 从 20 ns 真 Save 正常推进至 40 ns，wall time 523 s；40 ns 主 restart SHA-256 为 `1fc038f84af9021d404069b73fef0d057209c152cf1e312d5995b45aa5fc514f`。
- attempt11 使用逐段唯一文件前缀保存 92–108 ps 的 16 个场状态。按 W-2024.09 手册的二维默认厚度 `d=1 µm` 积分得到 `8.067321 pC`，相对名义 `7.775 pC` 偏差 `+3.7598%`，通过预设 5% 守恒门。
- 早期 98–102 ps 的 `5.162882 pC` 仅是截断窗口观察，不代表全脉冲不守恒。
- pre-strike Tmax 为约 324.633 K，说明 2500 V 热稳态 restart 已存在约 24.6 K 自热；该差异进入后续物理敏感性诊断。

## 7. 40–60 ns 求解器 A/B

| 方案 | 唯一数值变化 | 结果 | wall time | Newton 重试 | 决策 |
|---|---|---|---:|---:|---|
| A | 基线：Iterations 25、Increment 1.4、Extrapolate | 完成 60 ns | 817 s | 8 | 基线 |
| B1 | Iterations 25→15 | 完成 60 ns | 1037 s | 12 | 拒绝：慢 26.9% |
| B2 | 在 B1 上 Increment 1.4→1.2 | 预算仅到 51.746 ns | 1200 s | 12 | `INDETERMINATE(time_budget_exhausted)` |
| B3 | 在 A 上仅关闭瞬态 Extrapolate | 完成 60 ns | 749.94 s | 6 | 拒绝：仅改善 8.2%，低于 10% 门槛 |

A 与 B1 在 50/60 ns 的 Ic、Tmax、功率差异均小于 2%，趋势一致，证明 B1 物理等价但性能更差。B3 在 60 ns 的 Ic 为 `7.7945e-4 A/µm`、Tmax 336.122 K，与 A 的 `7.6042e-4 A/µm`、335.777 K 保持同一增长趋势；性能改善不足以接受。当前保留 A 为正式基线，未启动 A02。

## 8. A01 核心轨迹网格减半：DC 恢复与瞬态复核

- 新的不可覆盖 mesh variant 仅将轨迹核心四个步长均减半；网格为 70,276 点、139,710 单元，SHA-256 `eaffd577...f650d268`。差异表：`data/a01_meshhalf_mesh_diff.csv`。
- attempt12 在新网格上从零单线程正常 DC ramp，1200 s 硬预算耗尽；最后收敛的 Collector 电压仅 22.17 V，Tmax 300.013 K，没有生成真 Save。
- `attempt13_meshhalf_dcbench_t1`、`attempt14_meshhalf_dcbench_t2`、`attempt15_meshhalf_dcbench_t4` 的 1/2/4 线程等预算探针均在 300 s 内只到 0.3125 V，轨迹等价；2/4 线程 CPU 消耗更高且无进度收益，故拒绝并行方案并保留单线程。线程探针只作性能判断，不作物理分类。
- attempt13 保持同一网格、物理和 solver controls，只增加 100/500/1000/1500/2000/2500 V 原生 Save。3600 s 预算耗尽时最后收敛至 718.7 V、Tmax 304.138 K；保留了有效的 100 V 与 500 V Save。分类仍为 `INDETERMINATE(time_budget_exhausted)`，不是网格或物理结论。
- attempt17 从 attempt13 的同网格 500 V 真 Save 单线程续跑，正常完成 1000/1500/2000/2500 V；wall time 3607 s，2500 V 时 Tmax 325.798 K。2500 V 主状态 SHA-256 为 `d7d8985c...e84bfad6`，DC 前置门关闭。
- attempt18 使用该 2500 V 新网格 Save 和正式 solver 方案 A 完成 0–60 ns 瞬态；`EXIT_CODE=0`，wall time 9193 s，0.4/10/40/50/60 ns 主 Save 与 circuit Save 均成对且通过 SHA-256 核验。
- 半步网格相对基准网格的 Ic 在 10/40 ns 通过 2% 门，在 50/60 ns 分别相差约 6.19%/15.23% 并失败；两者均保持增长趋势。
- 40/60 ns 的沿轨迹 ElectricField、ImpactIonization 和 p-body/n+ 电势代理已按同一口径提取；局部雪崩峰值和位置显著分叉。该分支关闭为 `MESH_SENSITIVE`，不是 `mesh_converged`，也不是 SEB 分类。

## 9. `Wt_hi` 有界敏感性

- attempt19 仅把 `Wt_hi` 从 0.1 改为 0.2 µm，正常完成 60 ns，wall time 2737 s。60 ns Ic 为 `6.18942e-4 A/µm`，相对 0.1 µm 降低约 18.61%，但仍持续增长。
- attempt20 对 0.2 µm 完成 92–108 ps 电荷审计：`8.112455 pC`，相对名义值偏差 `+4.340%`，通过 5% 门。
- attempt21 将 `Wt_hi` 从 0.2 改为 0.5 µm，正常完成 60 ns，wall time 3551 s；60 ns Ic 为 `6.22856e-4 A/µm`，与 0.2 µm 仅相差约 0.63%，展宽效应已趋于饱和，增长趋势未逆转。
- attempt22 对 0.5 µm 完成电荷审计：`8.131790 pC`，偏差 `+4.589%`，通过 5% 门。
- 结论：`Wt_hi` 是幅值敏感参数，但不能解释论文 A01 的衰减趋势；不得选择某一宽度宣称复现成功。

## 10. 初始热状态敏感性

- attempt23 在相同 300 K 热接触和相同物理模型下，DC 阶段不求解 Temperature，使 2500 V 状态保持均匀 300 K；该状态明确标记为非热稳态诊断，不替代正式基线。
- attempt24 从该冷态恢复完整热方程并完成 60 ns，wall time 3282 s。10/40/50/60 ns Tmax 为 300.066/306.304/321.426/339.677 K。
- 冷态 60 ns Ic 为 `8.12775e-4 A/µm`，高于热稳态基准约 6.9%，同样持续增长。
- 结论：约 324.6 K 的 pre-strike 自热不是当前趋势偏差的主因；冷态热松弛不能作为追论文结果的正式模型替换。

## 11. 局部位置敏感性

- attempt25/26 仅把轨迹 Y 从 3.5 µm 改为 3.4/3.6 µm，均位于已有核心加密范围，分别在 3093/2423 s 内正常完成 60 ns。
- Y=3.4/3.5/3.6 µm 的 60 ns Ic 分别为 `6.18865e-4 / 7.60422e-4 / 6.22172e-4 A/µm`；两侧偏移相对中心均低约 18%，但三个位置全部持续增长。
- 这只是 A01 偏差诊断，不等同于预注册中的 Y=3.05/3.5/4.2 µm 正式位置扫描。

## 12. A01 三字段关闭

- `physical_classification = INDETERMINATE(time_window_short_and_mesh_sensitive)`：没有覆盖 1 µs，且晚时刻响应未网格收敛。
- `anchor_match = ANCHOR_MISMATCH`：所有已运行诊断仍与论文“约 10 ns 后衰减”相反。
- `mesh_assessment = MESH_SENSITIVE`：50/60 ns 端电流和局部雪崩场均未通过收敛要求。
- A02 及完整扫描标记为 `NOT_ENTERED(anchor_gate_failed)`；PA01 并行探索未进入。